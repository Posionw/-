<view class="main">
   <!-- 头部  -->
  <view class='head'>
    <view class="person_name">
      <image src='/image/name.jpg'></image>
    </view>
    <view class='person_con'>
      <view class='name'>Ccli西西里餐厅</view>
      <!-- 评论 -->
      <view class='pl'>
        <view class='pl_l'>6号桌</view>
        <view class='pl_c'>6人</view>
        <view class='pl_r'>下单时间 2017-12－20  18:30</view>
      </view>
      <!-- 会员 -->
      <!-- <view class='huiyuan'>
        <view class='huiyuan_l'>新会员尊享：消费8.8折／先领券再消费</view>
        <view class='huiyuan_r'>立即加入</view>
      </view> -->
    </view>
  </view>
  <!-- 价格  -->
  <view class='zprice'>
    <view class='zprice_k'>
      <view class='zprice_k_l'>订单金额</view>
      <view class='zprice_k_r'>￥{{totalPrice}}</view>
    </view>
    <view class='zprice_k2'>
      <view class='zprice_k_l'>实付金额</view>
      <view class='zprice_k_r'>￥{{totalPrice}}</view>
    </view>
  </view>




    <view wx:if="{{hasList}}">
        <view class="cart-box">
            <view class="cart-list" wx:for="{{carts}}" wx:key="{{index}}">
                <icon wx:if="{{item.selected}}" type="success" color="red" data-index="{{index}}"  class="cart-pro-select" bindtap="selectList"/>
                <icon wx:else type="circle" class="cart-pro-select" data-index="{{index}}" bindtap="selectList"/>
                <navigator url="../details/details?id={{item.gid}}"><image class="cart-thumb" src="{{item.pic}}"></image></navigator>
                <text class="cart-pro-name">{{item.name}}</text>
                <text class="cart-pro-price">￥{{item.currentPrice}}</text>
                <view class="cart-count-box">
                    <text class="cart-count-down" bindtap="minusCount" data-obj="{{obj}}" data-index="{{index}}" data-gid="{{item.gid}}" data-current="{{item.tid}}">-</text>
                    <text class="cart-count-num">{{item.num}}</text>
                    <text class="cart-count-add" bindtap="addCount" data-index="{{index}}" data-gid="{{item.gid}}" data-current="{{item.tid}}">+</text>
                </view>
                <text class="cart-del" bindtap="deleteList" data-index="{{index}}">×</text>
            </view>
        </view>

        <view class="cart-footer">
            <icon wx:if="{{selectAllStatus}}" type="success_circle" color="#fff" class="total-select" bindtap="selectAll"/>
            <icon wx:else type="circle" color="#fff" class="total-select" bindtap="selectAll"/>
            <view class="order-icon">
                <navigator url="../orders/orders"><image src="/image/icon3.png"></image></navigator>
            </view>
            <text>全选</text>
            <text class="cart-toatl-price">￥{{totalPrice}}</text>
        </view>
    </view>
    <view wx:else>
        <view class="cart-no-data">购物车是空的哦~</view>
    </view>
</view>