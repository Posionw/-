<view class="search_no" wx:if="{{!vou.length}}">
        <view class="font_14"><image class="scimg" src="/image/kong.png"></image></view>
        <text>此处空空如也/(ㄒoㄒ)/~~</text>
  </view>  
  <view wx:else>

<radio-group class="radio-group" bindchange="radioChange">
    <!-- <label class="radio" wx:for="{{items}}"> -->
        
        <label class="orderDetails radio" wx:for="{{vou}}" wx:key="{{vou}}">
        
        <view class="orderListDetails" bindtap='dian' data-yid="{{item.vid}}" data-name='{{item.title}}(满{{item.full_money}}可用)' data-zs='{{item.full_money}}'>
          <image class="k-quan" src="/image/yhj2.png"></image>
          <view class="productInfo">
                <text class="x">¥</text>
                <text class="y">{{item.amount}}</text> 
          </view>
          <view class="pr">
                <text class="n">{{item.title}}</text>
                <text class="b">满{{item.full_money}}可用（{{item.desc}}）</text>
          </view>
          <view class="hh" bindtap="getvou" name='id' data-vid="{{item.id}}">
            <!-- <view class="hb" wx:if="{{item.point>0}}">积分兑换</view> 
            <view class="hb" wx:else>领取</view> -->
            <view class="hb"><radio value="{{item.amount}}" checked="{{item.checked}}"/></view>
          </view>
        </view>
        <view class="c">有效期：{{item.start_time}}-{{item.end_time}}</view>
      <!-- </view>	 -->
    </label>
</radio-group>
</view>
<view class='footc' bindtap="tiaozhuan" data-status="1">确认使用</view>